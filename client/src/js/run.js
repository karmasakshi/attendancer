'use strict';

angular

    .module('attendancer')

    .run(['ENV', 'VERSION', function (ENV, VERSION) {

        /* --- MODELS --- */

        /* --- FUNCTIONS --- */

        /* --- RUN --- */

    }]);